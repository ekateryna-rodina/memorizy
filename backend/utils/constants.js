// min and max interval to show up the term while training
const minIntervalRepetition = 14400;
const maxIntervalRepetition = 2592000;
export default { minIntervalRepetition, maxIntervalRepetition };
