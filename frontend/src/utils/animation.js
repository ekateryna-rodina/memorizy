"use strict";
const toggleLoginRegister = (activeScreen) => {
  if (activeScreen === "register") {
    console.log("register");
  } else if (activeScreen === "login") {
    console.log("login");
  } else {
    break;
  }
};
